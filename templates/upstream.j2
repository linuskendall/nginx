upstream {{item.upstream.name}} {
  {% for host in groups[item.upstream.server_group] %}
    server {{hostvars[host]['ansible_'+item.upstream.host_if]["ipv4"]["address"]}}:{{item.upstream.port}};    
  {% endfor %}
}
